Secret Memory Locker Reference Engine

This directory contains the standalone reference implementation
of the SML file format.

It allows users to recover encrypted files independently
of the main Secret Memory Locker application.

The code is intentionally kept small, readable and self-contained.
